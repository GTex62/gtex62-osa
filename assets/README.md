# OSA Local Assets

Shared binary assets have moved to `../../gtex62-shared-assets/`.

This directory remains as a temporary compatibility location while OSA finishes
the transition to shared asset path resolution.
