local SUITE_DIR = os.getenv("CONKY_SUITE_DIR") or (os.getenv("HOME") .. "/.config/conky/gtex62-osa")
local layout = dofile(SUITE_DIR .. "/theme/osa-layout.lua")
local theme = dofile(SUITE_DIR .. "/theme/osa-theme.lua")
local window = theme.window_size(layout.frame)

conky.config = {
  alignment = "middle_middle",
  xinerama_head = theme.monitor_head or 0,
  gap_x = 0,
  gap_y = 0,

  own_window = true,
  own_window_class = "Conky",
  own_window_type = "desktop",
  own_window_hints = "undecorated,sticky,skip_taskbar,skip_pager,below",
  own_window_argb_visual = true,
  own_window_argb_value = 0,
  own_window_transparent = true,
  background = false,

  minimum_width = window.width,
  maximum_width = window.width,
  minimum_height = window.height,

  double_buffer = true,
  update_interval = 0.5,
  draw_shades = false,
  draw_borders = false,
  draw_outline = false,
  draw_graph_borders = false,
  use_xft = true,

  lua_load = SUITE_DIR .. "/lua/widgets/osa_main.lua",
  lua_draw_hook_pre = "draw_osa",
}

conky.text = [[]]
